<script>
	// @ts-nocheck

	/**
	 * Takes in props, determines if they need to be decrypted, and returns the decrypted props
	 */
	export let props;
	export let proxcryptor;
</script>

<slot {props} />
